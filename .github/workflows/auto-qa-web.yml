name: Auto QA — Web Smoke

# simulo .claude/agents/auto-qa.md 패턴 차용 (2026-05-27)
# PR 시 어드민/공개 라우트에 대한 시각·인터랙션 회귀 자동 탐지.
# 빌드 통과 ≠ 작동 — Playwright로 실제 렌더링 검증.

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/web/**"
      - "apps/fomo-web/**"
      - "packages/fomo-core/**"
      - "packages/shared/**"
      - ".github/workflows/auto-qa-web.yml"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # production build 안전 모드 — DB/외부 API 의존 없는 라우트만 검증
      DATABASE_URL: "postgresql://noop:noop@localhost:5432/noop"
      AI_API_URL: ""
      AI_API_KEY: ""
      AI_MODEL: "openai/gpt-4.1-mini"
      ADMIN_JWT_SECRET: "ci-only-dummy-secret-32-bytes-minimum-length"
      ADMIN_PASSWORD_HASH: "$2b$10$ci.dummy.hash.never.used.during.smoke"
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install workspace deps
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps
        working-directory: apps/web

      - name: Build web (production)
        run: npm run build:web

      - name: Run smoke tests
        run: npm run e2e
        working-directory: apps/web

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/web/playwright-report/
          retention-days: 14

      - name: Comment PR on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              '🤖 **Auto QA — Web Smoke 실패**',
              '',
              'production build 위에서 Playwright smoke 테스트가 실패했습니다.',
              '`빌드 통과 ≠ 작동` — 실제 렌더링/인터랙션 회귀 가능성.',
              '',
              `상세 리포트: [playwright-report 아티팩트](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              '',
              '체크 항목:',
              '- HTTP 200 응답',
              '- body 높이 > 0 (빈 화면 아님)',
              '- 콘솔 에러 0 (favicon/Yahoo 제외)',
              '- 어드민 미인증 시 적절한 처리',
            ].join('\n');
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            });

  # 사용자 화면 앱(apps/fomo-web) 렌더 스모크 (WO-SUB-04 Q3).
  #
  # 이 잡이 없던 동안 **사용자가 실제로 보는 앱에는 렌더 수준 CI 검증이 전혀 없었다.**
  # WO-SUB-04 첫 프리뷰에서 "미분류 칸에 빈 상자가 남는" 결함이 나왔는데 빌드·타입체크·
  # 유닛테스트는 전부 통과한 상태였다 — 서버를 띄워 렌더해보고서야 잡혔다.
  # 완료 조건 2(빈 박스로 남지 않을 것)를 게이트로 지키려면 여기서 봐야 한다.
  fomo-web-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install workspace deps
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps
        working-directory: apps/fomo-web

      - name: Build fomo-web (production)
        run: npx next build
        working-directory: apps/fomo-web

      - name: Run render smoke
        run: npm run e2e
        working-directory: apps/fomo-web

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fomo-web-playwright-report
          path: apps/fomo-web/playwright-report/
          retention-days: 14
