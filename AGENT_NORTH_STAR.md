# 🌟 Agent North Star

> 이 파일은 모든 에이전트가 매일 새벽 7시(KST)에 읽는 **단일 진실의 원천**입니다.
> 사용자가 직접 편집합니다. 에이전트는 절대 수정하지 않습니다.
> 매주 월요일 아침 사용자가 "이번 주 테마"와 "측정 지표"를 갱신합니다.

---

## 🎯 이번 주 테마 (Weekly Theme)

**카드 뽑기 → 결과 화면의 의식(ritual) 경험 완성**

- **시작일**: 2026-05-22
- **종료일**: 2026-05-29
- **CEO 한 줄 메시지**: "이번 주 모든 제안은 사용자가 '카드를 뽑는 순간부터 결과를 받기까지'의 흐름을 의식처럼 느끼게 하는 것에만 집중한다. 다른 영역은 다음 주로 미룬다."

---

## 🧪 핵심 가설

> 카드 뽑기 → 결과 화면 흐름이 "그냥 화면 전환"이 아닌 "의식"으로 느껴지면,
> D7 리텐션과 크레딧 재구매율이 동시에 오른다.

---

## 📊 측정 지표 (이번 주 목표)

| 지표 | 현재 (추정) | 목표 |
|---|---|---|
| 카드 뽑기 → 결과 화면 평균 체류 시간 | 미측정 | +30% (기준선 수립 후) |
| 결과 화면 "다시 뽑기" 또는 "공유" 클릭률 | 미측정 | +15% |
| 앱스토어 리뷰에서 "몰입감/신비로움" 키워드 등장 빈도 | 0건 | 측정 시작 |

→ 측정 인프라가 없는 항목은 **이번 주 안에 측정 시작**이 곧 성공이다.

---

## 🚫 이번 주 손대지 않을 것 (Out of Scope)

다음 영역의 제안은 자동 킬리스트로 간주합니다. 에이전트는 이 영역에 대한 제안을 만들지 마세요.

- 사주팔자 추가 (보류)
- 카카오톡 공유 기능 (보류)
- 어드민 UI 개선 (보류)
- 새 결제 상품 / IAP 변경 (안정 우선)
- 신규 화면 추가 (기존 화면 강화 우선)
- 새 인프라 도입 (Redis 등 — 비용 정당화 안 됨)

---

## 📐 프로젝트 사실 규약 (에이전트 필독 — 잘못된 가정 금지)

다음은 **검증된 사실**입니다. 이와 모순되는 제안은 잘못된 가설로 간주되어 거부됩니다.

### 인프라/스택
- DB 마이그레이션은 **`prisma migrate`가 아닌 `prisma db push`**로 운영한다. → "마이그레이션 미배포" 같은 제안은 잘못된 가설.
- React Native는 **Expo Go 환경** — `react-native-reanimated` 사용 불가. `Animated` API만 허용.
- packages/tarot-core 의 현재 prompt 버전: `interpret-v1.1.0.ts`.

### 이미 구현된 것 (재제안 금지)
- JWT HttpOnly 쿠키 (7일 만료)
- IAP 서버 검증 (RevenueCat 연동)
- HMAC nonce 광고 검증
- API 응답 캐싱 (4-tier — market 5분, interpret 4-tier)
- 카드 뽑기 7장 부채꼴 선택 UI
- 7일 스트릭 보상 시스템
- 공유 보상 (1일 1크레딧)
- 앱스토어 리뷰 요청 프롬프트

### 인지하고 있지만 이번 주는 보류
- AdMob 프로덕션 ID (인지됨 — 이번 주 우선순위 아님)
- E2E 테스트 부재 (의식 경험 완성 후 처리)
- 트래킹 도구 부재 (현재 console.log — 다음 주)

---

## ✍️ 제안 작성 절대 규칙 (모든 에이전트 공통)

### 표현
- ❌ **금지 어휘**: "가능성이 있음", "우려됩니다", "수도 있습니다", "보입니다", "~할 것 같습니다"
- ✅ **요구 어휘**: 단정형 + 근거. 근거가 없으면 제안하지 마세요.

### 근거
- 모든 "현재 문제" 진술은 **파일 경로 + 줄 번호** 또는 **메트릭 수치** 또는 **사용자 피드백 인용** 중 하나를 반드시 포함해야 합니다.
- 추측만으로 제안 금지. 근거 없으면 "조사 필요 — 다음 주" 로 미루세요.

### 액션 단위
- 제안 1개는 **1주일 내 단일 PR로 완료 가능한 범위**여야 합니다.
- "리팩토링", "전반적 개선" 같은 추상 표현 금지. 함수명/파일 경로/구체 구현 명시.

### North Star 정렬
- 제안 마지막에 반드시 **"이 제안이 이번 주 테마와 어떻게 연결되는가"** 1문장 포함.
- 연결 안 되면 "다음 주 후보"로 표시하고 본 제안에서 제외.

---

## 📚 직전 주 회고 (사용자가 매주 갱신)

### 5/15 ~ 5/21
- 완료: 카드 뽑기 7장 부채꼴 UI, 7일 스트릭, IAP 안정화
- 문제 발견: 에이전트 제안이 산발적 (서로 다른 방향), 같은 주제 반복, 근거 없는 추측 제안 많음
- 5/22부터 적용: North Star 도입, 자기 history 주입, 근거 강제 템플릿

---

## 🔄 갱신 방법 (사용자용)

매주 월요일 아침:
1. "이번 주 테마" 한 줄 갱신
2. 측정 지표 현재값 업데이트
3. Out of Scope 항목 조정
4. 직전 주 회고 추가

수동 트리거: `gh workflow run idea-proposal.yml` → 새 테마로 즉시 회의 실행.
