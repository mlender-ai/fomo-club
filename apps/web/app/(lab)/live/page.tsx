import { LabEmpty } from "../LabEmpty";

/** `/live` — 전광판. 전략별 성과 표는 LAB-08 이 만든다. */
export default function LivePage() {
  return (
    <>
      <h1 className="lab-title">전광판</h1>
      <LabEmpty note="LAB-07 페이퍼 실행기 · LAB-08 화면" />
    </>
  );
}
