# 거시 지표 임계 — 측정 결과

> **이 문서는 `scripts/macro-calibrate.ts` 가 생성한다. 손으로 고치지 않는다.**
> 값을 바꾸려면 스크립트를 다시 돌린다. 감으로 정한 임계는 카드가 안 나올 때 슬금슬금
> 낮추게 되고, 그러면 임계가 있으나 마나가 된다(MACRO-01 §C-1 · 하지 말 것).

- 측정 구간: 최근 730일
- `movePct` = 연속 3일 이상인 날의 **누적 변동률 |%| P50**
- `spikePct` = **하루 변동률 |%| P97**

## 백분위를 어떻게 골랐나

처음엔 P90/P97 로 잡았다. 재보니 합산 **하루 0.71건** 이었다 — 하루 1~3장을 내야 하는데
후보가 그것뿐이면 카드가 없는 날이 대부분이 된다.

"카드가 적으니 임계를 낮추자" 는 금지다. 대신 **목표 후보 수를 먼저 정하고 그걸 만족하는
가장 엄격한 백분위를 찾았다** — 목표 하루 2건. 고르는 근거는 감이 아니라
「하루 카드 1~3장」이라는 제품 요구다.

| 연속 임계 | 하루 후보 |
|---|---|
| P95 | 0.58건 |
| P90 | 0.71건 |
| P85 | 0.84건 |
| P80 | 0.97건 |
| P75 | 1.11건 |
| P70 | 1.24건 |
| P65 | 1.38건 |
| P60 | 1.51건 |
| P55 | 1.65건 |
| P50 | 1.79건 |

**고른 값: P50 — 하루 1.79건.**

> ⚠️ **바닥(P50)까지 내려도 목표 2건에 못 미친다.**
>
> 여기서 임계를 더 낮추지 않는다. 병목은 임계가 아니라 **`MACRO_MIN_STREAK = 3`**(3일 연속)이다 —
> 애초에 3일 이상 같은 방향인 날이 지표당 20~27% 뿐이다. 임계를 0으로 놔도 후보는 그 이상
> 안 나온다. 카드를 늘리려면 임계가 아니라 **지표를 더 늘려야** 한다.

## 급변(`spikePct`)을 따로 고정한 이유

`spikePct` 는 **P97 고정**이다. 목표 후보 수를 맞추느라 같이 내렸더니 P79 가 나왔는데,
그러면 **닷새에 하루가 「급변」** 이 된다. 화면에 `하루에 0.7% 올랐어요` 를 급변이라고 쓰는 셈이고
그건 거짓말에 가깝다. 급변은 드물어야 급변이다. 후보 수는 연속 임계로만 맞춘다.

| 지표 | 관측 | 기간 | 연속 표본 | movePct | spikePct | 후보 비율 |
|---|---|---|---|---|---|---|
| `usdkrw` | 520 | 2024-07-15 ~ 2026-09-01 | 89 | 1.4 | 1.4 | 10.8% |
| `jpykrw` | 520 | 2024-07-15 ~ 2026-09-01 | 117 | 1.6 | 1.6 | 13.9% |
| `ktb3y` | 525 | 2024-07-05 ~ 2026-08-31 | 56 | 3.4 | 3.2 | 8.2% |
| `ust10y` | 497 | 2024-09-03 ~ 2026-08-28 | 90 | 3.1 | 2.5 | 12.1% |
| `ust2y` | 497 | 2024-09-03 ~ 2026-08-28 | 53 | 3.5 | 3.1 | 7.9% |
| `yieldcurve` | 498 | 2024-09-03 ~ 2026-08-31 | 52 | 17.5 | 60 | 8.5% |
| `corp3y` | 525 | 2024-07-05 ~ 2026-08-31 | 56 | 2.3 | 2.5 | 8.2% |
| `creditspread` | 525 | 2024-07-05 ~ 2026-08-31 | 21 | 4.9 | 4.1 | 5.2% |
| `kospi` | 520 | 2024-07-10 ~ 2026-08-31 | 139 | 5.1 | 6.6 | 16.0% |
| `kosdaq` | 520 | 2024-07-10 ~ 2026-08-31 | 129 | 4.6 | 6 | 15.4% |
| `sp500` | 499 | 2024-09-03 ~ 2026-08-28 | 127 | 2.3 | 2.2 | 15.7% |
| `nasdaq` | 499 | 2024-09-03 ~ 2026-08-28 | 132 | 3.4 | 2.8 | 15.7% |
| `vix` | 514 | 2024-09-02 ~ 2026-08-28 | 100 | 17.8 | 18.8 | 12.9% |
| `oil` | 525 | 2024-07-30 ~ 2026-08-31 | 127 | 6.2 | 7 | 14.7% |
| `gold` | 750 | 2023-08-04 ~ 2026-09-01 | 187 | 3.1 | 3.1 | 14.6% |

**지표 15종 합산 기대 후보 = 하루 1.79건.**
연결(업종 2곳 이상) 필터와 분류별 상한(2장)을 지나면 이보다 줄어들고, 전체 상한은 3장이다(§C-2).

## 운영 소스 (`apps/web/lib/macro-collect.ts`)

| 갈래 | 지표 |
|---|---|
| 네이버 국내지수 | `kospi` `kosdaq` |
| 네이버 해외지수 | `sp500` `nasdaq` `vix` |
| 네이버 환율 | `usdkrw` `jpykrw` |
| 네이버 일별 시세표 | `ktb3y` `corp3y` `oil` `gold` |
| 미 재무부 일별 CSV | `ust10y` `ust2y` |
| 계산 | `creditspread`(회사채−국고채) · `yieldcurve`(미 국채 10년−2년) |

### FRED 를 걷어냈다

원래 미국 지표는 FRED 에서 받았다. 그런데 실제로 굽혀 보니 **화요일에 미국 지표 6종이
전부 「4일 전 기준」** 이라 신선도 게이트에 걸렸다(2026-09-01 실측). 지표를 15종으로
늘려 놓고 그중 6종이 주 초에 죽으면 늘린 의미가 없다.

| 지표 | FRED | 원본 |
|---|---|---|
| 미 국채 10년 | 08-28 | **08-31** (재무부) |
| S&P 500 | 08-28 | **08-31** (네이버 해외지수) |
| WTI 유가 | 08-25 | **08-31** (네이버) |

옮겨 실은 것을 받을 이유가 없다. FRED 는 공개 도메인이라 좋았지만 **느린 게 문제였다.**

> 이 스크립트는 **측정용으로만** FRED 를 계속 쓴다 — 임계는 분포를 보는 것이라 하루이틀
> 지연이 상관없고, 2년치를 한 번에 주는 편이 낫다. 운영 수집과 소스가 다른 것은 의도다.

## 미 기준금리를 뺀 이유

`FEDFUNDS` 는 **월간** 시리즈다(2년에 23관측). 연속·급변을 잴 표본이 안 되고, 최신값이
언제나 한 달 이상 묵어 있어 신선도 게이트(§B-3)에 걸린다. 매일 보는 지표 목록에서 뺀다 —
만들어 놓고 영영 안 나오는 자리를 남기지 않는다.
## 실제로 하루 몇 장이 나오나 — 70일 재생 (2026-09-01)

임계만 보면 후보 수는 알아도 카드 수는 모른다. 뒤에 **연결 필터**(업종·시장 2곳 이상)와
**분류별 상한**(2장)이 있기 때문이다. 그래서 과거 70거래일을 그대로 재생해 셌다.
재생 코드는 **`scripts/macro-backtest.ts`** 다. 각 날짜에서 **그날까지만** 보고 판정하므로
미래를 보지 않는다. 다시 돌리면 같은 방식으로 다시 센다.

| 항목 | 값 |
|---|---|
| 재생 구간 | 70거래일 |
| 총 카드 | 124장 |
| **하루 평균** | **1.77장** |
| 1~3장인 날 | 58일 (83%) |
| 0장인 날 | 12일 (17%) |
| 상한(3장) 초과 | **0일** |

분포: `0장 12일 · 1장 16일 · 2장 18일 · 3장 24일`

### 지표별 등장 횟수

| 지표 | 횟수 | 지표 | 횟수 |
|---|---|---|---|
| `oil` | 29 | `usdkrw` | 6 |
| `kosdaq` | 23 | `yieldcurve` | 5 |
| `kospi` | 19 | `corp3y` | 3 |
| `jpykrw` | 19 | `creditspread` | 1 |
| `vix` | 7 | | |
| `ust10y` | 6 | `ust2y` | 6 |

**11종이 실제로 등장한다.** 종전에 유가 하나만 나오던 것과 비교하면 이 작업의 결과가
여기 있다. 유가가 여전히 1위인 것은 유가가 원래 많이 움직이기 때문이지 편애가 아니다.

### 사건 종류별

| 종류 | 횟수 |
|---|---|
| `streak` (연속 흐름) | 72 |
| `level` (기준선 통과) | 31 |
| `spike` (급변) | 21 |
| `inversion` (관계 역전) | 0 |

`inversion` 이 0인 것은 **이 구간에 미 장단기 금리가 안 뒤집혔기 때문**이다. 코드가 안
도는 게 아니라 사건이 없었다(단위 테스트가 그 경로를 따로 확인한다). 없는 사건을 만들지
않는다.

## 0장인 날을 어떻게 볼 것인가

17% 는 낮지 않다. 그래도 **조건에 맞는 게 없으면 0장**이 맞다 — 채우려고 임계를 낮추면
「환율이 0.1% 올랐어요」가 덱에 들어오고, 그건 카드가 아니라 소음이다.

늘리고 싶으면 임계가 아니라 **지표를 더 늘린다.** 지금 못 넣은 것들:

| 지표 | 왜 못 넣었나 |
|---|---|
| 미 기준금리 | 월간 시리즈 — 표본도 신선도도 안 된다 |
| 반도체 D램 현물가 | 키 없이 일별로 주는 공개 소스를 못 찾았다 |
| 구리 | 네이버 `metals` 에 일별 시세표가 없다. FRED 는 월간(`PCOPPUSDM`) |
| 달러인덱스 | 코드(`FX_USDX`)는 있는데 일별 시세 표가 안 열린다 |
