# AC 부채 원장 (WO-SUB-03.5 PART B-3)

**AC 미충족 상태로 머지하려면 여기에 사전 등재하고 정산 시점을 명시한다.** 사후 기록이 아니라 사전 등재다.
"머지됐다"를 "충족됐다"로 쓰지 않기 위한 장치다.

| WO | AC | 목표 | 머지 시점 실제 | 정산 결과 | 정산일 |
|---|---|---|---|---|---|
| WO-SUB-02 | AC-2 일치율 | ≥ 85% | 72% (미충족) | **충족** — 분해 집계 시 94.7%. 머지 시점 계산이 미분류를 불일치에 포함한 오류였다 | 2026-07-29 |
| WO-SUB-02 | AC-2 위험 오분류율 | ≤ 5% | 4% (충족으로 기록) | **미충족** — 분모를 `정분류+오분류`로 바로잡으면 5.3%. 머지 시점 계산이 분모를 전체로 잡은 오류였다 | 2026-07-29 (미해소) |
| WO-SUB-03 | AC-1 골든셋 사람 검증 ≥90% | ≥ 90% | **측정 불가** (LLM 429) | 429 해소·페이싱 재실행 완료돼도 **AC#1 판정 금지** — 입력 크기 미확정 상태의 측정이라 무효(아래 참조) | **차단 중** |

---

## 미해소 항목

### WO-SUB-03 AC-1 — 측정 순서가 거꾸로였다 (2026-08-01 사용자 판정)

3,500자 입력이 청크의 **중위 89%** 를 버린다(Rhinebeck 122개 → 2개). 10-K Item 1 의 11% 만 보고
사업 실체를 합성하고 있다는 뜻이다. 그 상태로 재실행해 통과율을 재면 **AC#1 을 재는 것이 아니라
"89% 잘린 파이프라인의 통과율"을 재는 것**이다.

> 오히려 통과하면 그게 더 이상하다. 문서 11% 만 보고 나온 문장이 검증을 통과한다면
> 그 문장이 **어느 문서에나 해당하는 일반론**일 가능성이 크다 — WO-SUB-03 이 없애려던 것이 정확히 그것이다.

**확정된 순서**

1. 페이싱 준수 재실행 결과는 `3,500자 조건 하 결과` 로만 라벨링한다. **AC#1 충족 판정 금지**
2. PART C-1(입력 크기 A/B — 10종목, 24k vs 3.5k)을 **먼저** 돌린다
3. C-1 결과로 입력 크기를 **품질 기준으로** 확정한다
4. 그 크기로 재실행한 결과만 AC#1 이다

인프라 제약(TPM)이 정한 품질 수준을 제품 사양으로 받아들이지 않는다.

### WO-SUB-02 AC-2 위험 오분류율 5.3% (목표 5%)

- 초과분 0.3%p = 4건 중 1건 수준
- 4건 중 2건(Rivian·Lucid)이 **미도출 임계값 `θ_hypergrowth`** 에서 나온다
- 해소 경로: `WO-SUB-02R` PART E-2 임계값 재도출
- **이 상태로 04 를 착수하지 않는다** — 02R 이 후행 차단을 걸어둔 이유가 이것이다

### WO-SUB-03 AC-1 사람 검증

- 에이전트는 판정하지 않는다. 대조표(`EVAL_golden30.md`)를 제출하는 데까지가 역할이다
- 골든셋 완주 후 판정 칸이 채워질 수 있는 상태가 되면 사람 검토 요청

### WO-SUB-08 — ①③ 배선 외 전부 (2026-08-05 등재)

지시로 완료 조건을 재정의했다: **08 완료 = 프로덕션 배포 후 화면에서 ①③ 카드가 보이는 것.**
AC 100% 를 목표로 하지 않는다 — 며칠째 그것 때문에 끝나지 않았다. 아래는 등재하고 넘어간다.
정산 시점: **배포 후.**

| AC | 상태 | 사유 / 해소 경로 |
|---|---|---|
| 1. 텍스트 예산 초과 시 빌드 실패 | ❌ | 빌드 타임 검사기 미구현. 슬롯1 문장은 `MAX_SLOT_CHARS=60` 으로 생성 단계에서 이미 상한이 있어 런타임 잘림은 없다(AC#2 는 충족). 검사기 자체는 부채 |
| 3. 슬롯 조합 4종 × 2뷰포트 = 8 스냅샷 | ❌ | `fomo-web-smoke` 가 프리뷰 라우트만 본다. 덱 카드 조합 스냅샷은 부채 |
| 6. 카드 앞면 콘텐츠 영역 ≥ 75% | ❌ | 미측정. ③ 이 붙은 카드는 올라갔을 것이나 **①만 카드(38.6%)는 그대로다** — 측정 자체가 두 모집단을 나눠야 의미가 있다 |
| 7. 덱 헤드라인 설정값 분리 | ❌ | 손대지 않았다. §8 이 "결정하지 않는다" 라 문구를 건드릴 이유가 없었고, 분리 작업만 남았다 |
| WO-SUB-04 AC#7 차트 데이터 배치 사전계산 | ❌ | `/api/fomo/card-slots` 가 **요청 시점에 저장 레코드에서 조립**한다. 외부 소스를 부르지 않아 결정론은 유지되지만, 종목 수가 늘면 응답이 느려진다. 크론 사전계산은 부채 |
| WO-SUB-04 AC#8 A/B 설계 문서 | ❌ | 착수 조건 3("A/B 는 나중")에 따른 **의도된** 미충족 |

**충족된 것**(부채가 아니다): AC#2(런타임 말줄임 경로 없음) · AC#4(빈 섹션 0) · AC#5(재무 한눈에) ·
AC#8(3층 비율 대시보드 = `/api/fomo/card-slots/coverage`).

### WO-SUB-08 AC#5 "재무 한눈에" — **이미 닫혀 있었다**

WO §5 는 이 섹션을 "현재 빈 헤더로 남아있는 섹션" 이라고 적지만 **그 전제가 낡았다.**
`FinanceGlanceBlock` 은 `lines.length === 0` 에서 `null` 을 돌려주고(`KeywordDepthPage.tsx`),
두 호출부(`QuietPickDepth.tsx:328`·`KeywordDepthPage.tsx:2931`)가 **감싸는 섹션까지 조건부**로
두고 있다. 즉 선택지 (a)제거 로 이미 닫혀 있다 — 헤더만 남는 경우가 없다.

남은 선택은 (b)채우기 다: 지금 소스가 `StockBasics`(네이버·나스닥)이고 **팩트시트를 쓰지 않는다.**
KR 93.8% · CF 88.4% 를 확보했으니 팩트시트 기반으로 채우면 밀도가 올라간다.
**이번 범위에서 하지 않았다** — 08 완료 조건이 ①③ 배선이고, 스코프를 고정하라는 지시가 있었다.
해소 경로: `card-slots` 페이로드에 팩트시트 요약을 실어 디테일에서 렌더(같은 라우트 재사용).

### WO-SUB-08 — ①만 있는 카드 123장은 정상 상태다

`UNCLASSIFIED` 95 + `BANK_FINANCIAL` 19 + `MATURE_INCOME` 9 = **123장이 슬롯 ③ 축을 만들 수 없다.**
이 카드들이 지금과 동일하게 보이는 것이 **정상이고 억지로 채우지 않는다**(지시).

- `UNCLASSIFIED` 95 → `WO-SUB-02R` 카탈로그 재도출이 줄인다
- `BANK_FINANCIAL` 19(순이자이익) · `MATURE_INCOME` 9(주당배당금) → **소스에 계정 매핑이 없다.**
  데이터를 더 모아도 안 된다. 다른 지표로 바꿔 그리지 않는다
- 비율 추적: `/api/fomo/card-slots/coverage` 가 조합별 분포를 낸다. 슬롯 ③ 커버리지가
  올라갈 때 `①만` 이 줄어드는 것이 여기서 보인다

### WO-SUB-08 완료 확인 — 프로덕션 화면 (2026-08-05)

**완료 조건: 프로덕션 배포 후 화면에서 ①③ 카드가 보이는 것 → 충족.**

배포: 백엔드 `a466643` 자동 · 프론트 `vercel-production-deploy.yml` run 30962679849 성공
(별칭 `fomo-web-mlender-ais-projects.vercel.app`).

확인된 것(모바일 375×812, 다크):

| 슬롯 | 화면 |
|---|---|
| ① 트리거 | 훅 문장 + 신호칩 3개 + 스파크라인 ◆ |
| ③ 값의 위치 | 막대 `21 22 23 24 25 26` + `예상` 경계 + PBR 밴드 캡션 |
| 무효선 | 가려지지 않음(스크롤 영역 밖) |
| 배지 | **카드에 없음**(지시대로 디테일에만) |

콘솔 에러 0.

### 배포 후 화면 확인이 잡은 것 — 로컬·CI 로는 안 잡혔다

두 결함이 **빌드·타입체크·테스트 1,678건·스모크 5건을 전부 통과한 상태에서** 남아 있었고,
서버를 띄워 렌더해보고서야 보였다. 08 완료 조건에 "배포 후 화면 확인" 을 넣은 판단이 옳았다.

1. **무효선이 하단 버튼에 가려짐** — ③ 차트가 붙어 카드 높이를 넘겼다. 내 변경이 만든 회귀
2. **예상 막대 축 라벨 `202612`** — 실적 라벨(2자리)과 자릿수가 섞여 차트가 잘못 읽혔다

추가로, 첫 스크린샷에서 차트가 안 보여 "프로덕션 미반영" 으로 오독할 뻔했다. 실제로는
**슬롯이 별도 `useEffect` 라 첫 페인트에 없었던 것**이고 DOM 검사(`figure` 존재, `top=431`)로
구분했다. 스크린샷 한 장으로 판정하면 틀린다 — 네트워크·DOM 을 같이 봐야 한다.

### 오늘 덱의 ③ 은 1/10 이다 — 구조적 한계

유니버스 332장에서 ③ 이 199장(59.9%)인데 **덱 10장에서는 1장**이다. 우연이 아니다.

**덱은 "조용한 돈" 을 고르므로 정의상 커버리지가 낮은 소형·무명주를 뽑는다.** 그게 정확히
아키타입 분류가 안 되는 종목이다. 유니버스 수치는 과거 발행분 누적이라 대형주가 섞여 있어
**덱 화면을 예측하지 못한다.** 어제 live deck 2/10, 오늘 1/10 이 그 증거다.

사유(팩트시트 백필 후): `unclassified` 7 · `bar_series_unavailable` 2
- `unclassified` 7 → **02R 카탈로그 재도출**이 줄인다. 08 이 할 수 있는 것이 없다
- `bar_series_unavailable` 2 → BANK_FINANCIAL 순이자이익 매핑 부재. 데이터를 더 모아도 안 된다

즉 **08 의 진척은 이제 02R 과 슬롯② 배치에 달려 있다.** 카드 레이아웃 쪽에 남은 일이 아니다.

### STABLE_EARNINGS 배포 후 실측 — 덱 ③ 3/10 → 7/10 (2026-08-06)

02R 판정 카드의 결론(유형 부재)을 유형 **1개**로 구현하고 배포·실측했다.
배포 run 31059672336 · 브랜치 시뮬레이션 run 31059180727.

| 지표 | v1.1.0 | v1.2.0 |
|---|---|---|
| `no_rule_matched` | 82 | **22** |
| 유니버스 슬롯③ | 68.2% | **81.4%** |
| 오늘 덱 슬롯③ | 3/10 | **7/10** |
| `STABLE_EARNINGS` | — | 61장 · 슬롯③ **61/61(100%)** |

프로덕션 실측(`/api/fomo/card-slots`, `ruleset_version: archetype-v1.2.0`): 덱 10장 중 7장이
슬롯③ 렌더(STABLE_EARNINGS 5 · TURNAROUND_LOSS 1 · CYCLICAL_COMMODITY 1). 나머지 3장은
신규 발행분으로 팩트시트 백필 전이다. 카드 화면에 매출 막대 5개 + 축 라벨 + PER 밴드 캡션 +
경고문("관측 지점은 성장률보다 이익의 안정성과 값의 위치입니다") 확인.

**잔여 22건의 성격 — 카탈로그 공백은 4건뿐이다.**

| 잔여 | 건수 | 성격 |
|---|---|---|
| 순이익 TTM `null` | 13 | 규칙 도달 불가 — 팩트시트 결손(카탈로그 무관) |
| CAGR3Y `null` | 5 | 동일 |
| 흑자 · CAGR 5~15 · 배당>3 | **4** | `MATURE_INCOME`(CAGR<5 요구)과 `STABLE_EARNINGS`(배당≤3 요구) 사이의 실제 공백 |

→ **B2(549종목)는 신규 후보 검증용으로 축소 가능**(지시 기준 "20 이하면 150종목 내외").
결정은 사람. 잔여 4건은 `STABLE_EARNINGS` 의 배당 조건을 제거하면 흡수된다(규칙 순서상
`MATURE_INCOME` 이 먼저라 CAGR<5·배당>3 은 그대로 성숙형에 남는다) — 임계값 완화가 아니라
경계 정리이지만 **이것도 사람 결정으로 남긴다.**

### MATURE_INCOME 주당배당금 — 나눗셈이 아니라 소스에 있었다 (2026-08-06)

지시는 "`dividend_paid` 총액 ÷ `shares_outstanding`" 이었으나 **실측하니 그 경로가 위험했다**:
두 값 모두 **스칼라 1개**라(TTM 총액 · 현재 주식수) 연간 막대를 만들려면 과거 연도에 현재
주식수를 대야 하고, 분할·증자 이력이 있으면 틀린 값이 된다.

대신 **네이버 연간표가 `주당배당금` 행을 그대로 준다**(한전KPS 051600 실측: 2023 2,158 /
2024 2,469 / 2025 1,651원). 총액 역산 없이 `valuation.dps_annual` 로 싣는다.

- MATURE_INCOME 사유가 `bar_series_unavailable` → **`no_bar_data`** 로 바뀐다
  (축은 지원하고 그 종목에 값이 없는 상태 — 해소 경로가 백필이다)
- **US 는 빈 배열** — SEC `flows()` 가 분기 기간(MIN/MAX_QUARTER_DAYS)만 통과시켜 연간
  기간값을 받지 못한다. 연간 헬퍼가 필요하다(부채). 현재 유니버스 MATURE_INCOME 10장이
  전원 KR 이라 화면 영향 없음
- 커버리지 효과는 **팩트시트 백필 후** 나타난다(run 31059967355)

### 카드 앞면에서 ③ 이 접힘선 아래로 밀린다 (2026-08-06 관측 · 등재)

앞선 등재("캡션·경고문이 스크롤 아래")의 연장인데 **③ 차트 자체가 밀리는 카드**를 실측했다.

Albertsons 카드(STABLE_EARNINGS, 5/10) DOM 측정:
- 카드 스크롤 영역 227~524 · `scrollHeight 464 / clientHeight 297` → **스크롤 여유 167px**
- `figure` top **515** (컨테이너 하단 -9px) · 높이 176 → 첫 화면에 상단 몇 px 만 보인다

**INV-11 무효는 아니다** — PER 수치(밴드 캡션)와 경고문이 **함께** 접혀 있다(지시 기준:
"수치도 같이 아래면 문제없어"). 스크롤로 전부 도달한다.

다만 08 의 "카드 앞면에 ③ 을 올린다"는 목적이 **훅이 긴 카드에서는 달성되지 않는다.**
원인은 콘텐츠 예산 초과이고 08 AC#1(텍스트 예산 초과 시 빌드 실패)·AC#6(콘텐츠 영역 ≥75%)이
부채로 남아 있는 것과 같은 계열이다. 레이아웃 수술은 08 스코프이므로 이번 범위에서 하지 않았다.

### A-3 배포 후 실측 — 덱 ③ 1/10 → 3/10 (2026-08-06)

BANK 막대축을 자기자본으로 바꾼 뒤(`#1041`) **배포하고 실측했다.** A-3 단서가 "덱 3/10은
배포 후 실측할 것. 지금은 추정이야" 였고, 추정과 실측이 일치했다.

배포: production deploy run 31024658458 (backend·web 둘 다 success) · 측정: card-slot-coverage run 31024426851

| 지표 | 전(v1.0.0) | 후(v1.1.0) |
|---|---|---|
| 덱 슬롯③ | 1/10 | **3/10** |
| 덱 `bar_series_unavailable` | 2 | **0** |
| 유니버스 슬롯③ | 225/362 (62.2%) | **247/362 (68.2%)** |
| 유니버스 `BANK_FINANCIAL` ③ | 0/22 | **22/22 (100%)** |
| 유니버스 `bar_series_unavailable` | 32 | **10** (전량 `MATURE_INCOME` 주당배당금) |

프로덕션 화면 실측(모바일 375×812, 다크): 4/10 Rhinebeck Bancorp 카드에 축 라벨 **자기자본** +
막대 5개(21~25). 디테일 시트에 PBR 밴드 캡션(98% 구간) + 은행 경고문 표시.
`/api/fomo/card-slots` 응답에서 BANK 2장 `bar_metric: annual_equity` · `bar_label: 자기자본` ·
캡션 마지막 줄 `막대는 자기자본입니다.` 확인.

**남은 `bar_series_unavailable` 10장은 `MATURE_INCOME`(주당배당금)이다.** 이 축은
WO-SUB-04 표에 대안이 없어 A-3 과 같은 해법을 쓸 수 없다 — 부채로 유지한다.

### 카드 앞면 캡션·경고문이 첫 화면 밖이다 (2026-08-06 등재 · A-3 이 만든 것 아님)

실측 중 발견. 카드 앞면(compact)의 `figcaption`(캡션 1줄 + 경고문)이 카드 내부 스크롤
컨테이너(`min-h-0 flex-1 overflow-y-auto`)의 **아래쪽 밖**에 있다.

| 카드 | 축 | 캡션 위치 | 컨테이너 하단 | 내부 스크롤 여유 |
|---|---|---|---|---|
| 레인보우로보틱스 | 매출 | 517~619 | 536 | 83px |
| Rhinebeck Bancorp | 자기자본 | 564~647 | 531 | 119px |
| Baycom Corp | 자기자본 | 569~655 | 536 | 119px |

- **스크롤하면 도달한다**(여유 83~119px). 잘려서 사라지는 것이 아니다
- **매출 카드도 같다** — 축을 바꿔서 생긴 것이 아니라 원래 그렇다. A-3 무관
- 다만 INV-11(경고문 부착)의 취지가 화면에서 약해진다. 카드 앞면은 막대만 보이고
  값의 해석 문장은 스크롤 또는 디테일에서 본다
- 해소 경로: 08 계열 레이아웃 작업. 이번 범위에서 손대지 않았다(스코프 고정 지시)

### WO-SUB-07 [F] 1단계 — 기록만 먼저, 채점은 나중 (2026-08-06 등재)

WO-SUB-FINISH [F] 는 "발행 시점 기록을 05·06 기다리지 말고 이번 주에 시작" 을 **소급 불가**
항목으로 지정했다. 그래서 기록만 먼저 넣고 채점을 등재한다. 범위·근거는
`docs/audit/WO_SUB_07_publication_record.md`.

| 축소 AC | 상태 | 사유 / 해소 경로 |
|---|---|---|
| 1. 발행 시점 기록 append-only | ✅ | `payload.publication` 스탬프. 원장 자체가 append-only(트리거·RLS) |
| 2. `factsheet_hash` 로 발행 시점 복원 | ✅ | 해시 기록 + 기존 `readFactSheetSnapshot()` 경로 |
| 3. T+30 채점 1종 동작 | ❌ | 기록이 먼저다. 스탬프가 쌓인 뒤 붙인다. 정산 시점: 스탬프 7일 누적 후 |
| 4. 성적표 탭 축별 결과 + n 병기 | ❌ | 축이 05 에서 확정돼야 축별이 된다. 정산 시점: 05 완료 후 |

추가 부채(같은 시점 등재):
- **daily-30 `selection` 경로 스탬프 없음** — 성적표 계열은 `pickType=quiet` 라 1단계 범위 밖.
  daily-30 발행분은 스탬프 없는 행으로 남는다(소급 불가 — 그 사실을 여기 남긴다)
- **`invalidation_business` 전 행 `null`** — 06 산출물. `invalidation_business_status:
  "pending_wo_sub_06"` 로 "확보 못함" 과 "없음" 을 구분해 둠
- **`earnings_date` KR 전량 미확보** — 사전 공표 소스 부재(`no_source_kr`). 데이터를 더 모아도 안 된다

---

## 등재 규칙

1. AC 미충족 머지는 **머지 전에** 이 표에 등재한다
2. 정산 시점을 명시한다 — "나중에"는 등재가 아니다
3. 정산 결과가 **양방향**일 수 있다. 재측정으로 충족이 되기도 하고 미충족이 되기도 한다.
   유리한 쪽만 정정하지 않는다
