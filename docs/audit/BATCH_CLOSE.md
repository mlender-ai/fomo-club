# 실체화 배치 마감 — 막힌 것과 재개 조건

측정일 **2026-08-13** · 룰셋 `archetype-v1.2.0` · 독트린 `archetype-v1.4.0`

이 문서는 한 장이다. **무엇이 막혔고, 무엇이 풀리면 재개하는지**만 적는다.
개별 근거는 `AC_DEBT_LEDGER.md` 와 각 WO 리포트에 있다.

---

## 1. 이번 마감(WO-SUB-CLOSE)에서 한 것

| 파트 | 결과 |
|---|---|
| A — 대손충당금 전입액 수집 | **완료** (#1069). US 은행 **12/12 판정 가능**. BANK 무효 조건이 "수집 미배선"에서 실제 판정으로 넘어갔다 |
| B — 02R B2 재개 | **불필요 판정.** 전제가 이미 해소돼 있었다 — 아래 §2 |
| C-1 소급 스캔 역검증 | **완료** (#1070). 결함 3건 발견·수정 |
| C-2 가격 무효선 회귀 테스트 | **완료** (#1070). 원래 버그 주입 시 6건 실패 확인 |
| C-3 flaky 테스트 격리 | **이미 해소돼 있었다** — `docs/quality/FLAKY_TEST_AUDIT.md` |
| C-4 02 일치율 재측정 | **미실시** — 라벨 노후로 오해를 부르는 숫자가 나온다. 아래 §3 |
| C-5 C-1 입력 크기 A/B | **미실시** — 유료 호출. 아래 §3 |
| C-6 03 골든셋 사람 검증 | **미실시** — 사람이 필요하다 |

> **소급 스캔이 찾아낸 것**: 새로 붙인 종목 리스크 검사가 프로덕션에서 레코드 **1건**을 읽었다.
> 위반은 0건이지만 그 1건이라는 수 자체가 발견이다 — 06.5 의 합성 배치가 배선만 되고
> 실행되지 않았다. §3 에 등재. 검사 대상을 늘리자마자 나온 결과다.

---

## 2. PART B 를 하지 않은 이유 (실측)

지시서의 전제는 `no_rule_matched` **82건**과 "신규 유형 미신설"이었다. 둘 다 이미 달라져 있었다 —
`STABLE_EARNINGS` 가 독트린 v1.4.0 에 신설·배포된 상태다(WO-SUB-FINISH 2차).

| 지표 | 지시서 전제 | 실측(2026-08-13) |
|---|---|---|
| 유니버스 `no_rule_matched` | 82건 | **27건** |
| 그중 **실제 규칙 공백** | — | **4건** |
| 오늘 덱 미분류 | 7건 | **0건** (③ 없는 1장은 `no_factsheet` — 분류 문제가 아니다) |
| 유니버스 슬롯 ③ | — | 352/441 (79.8%) |

지시서 규칙은 "82건이 20건 이하로 줄면 B2 를 축소한다 · `no_rule_matched` 가 소수면
카탈로그를 늘려도 안 풀리는 것에 2~3일을 쓰지 않는다" 였다. **실제 공백 4건이므로 549종목도,
축소한 150종목도 모으지 않았다.**

잔여 27건의 분해:

| 성격 | 건수 | 카탈로그가 푸나 |
|---|---|---|
| 순이익·CAGR 이 `null` — 규칙에 **도달하지 못한다** | 23 | 아니오 — 데이터 결손 |
| 관측이 다 있는데 규칙이 없다 | **4** | 경계 판단 필요 — 아래 |

### 잔여 4건은 모두 **같은 구간**이다

| 종목 | 순이익 | CAGR 3Y | 배당률 |
|---|---|---|---|
| 일진파워 | 흑자 | 8.7 | 3.13 |
| NH농우바이오 | 흑자 | 6.0 | 3.64 |
| 한국전력 | 흑자 | 11.0 | 4.33 |
| SNT홀딩스 | 흑자 | 11.1 | 6.22 |

`MATURE_INCOME`(배당>3 **AND** CAGR<5)과 `STABLE_EARNINGS`(흑자 **AND** CAGR<15 **AND** 배당≤3)
사이의 삼각형 구멍이다 — **흑자 + 배당>3 + 5≤CAGR<15**.

**새 유형이 필요한 것이 아니다.** `STABLE_EARNINGS` 의 배당 조건 한 줄을 떼면 4건이 흡수되고,
`MATURE_INCOME` 이 규칙 순서상 먼저 평가되므로 기존 판정은 하나도 바뀌지 않는다.

**그런데 그게 맞는지는 판단 사안이라 하지 않았다.** 배당 6.22% 인 종목에 "이익 안정" 프레임을
씌우게 되고, `classify.ts` 의 주석이 정확히 그 위험을 경고한다("임계값을 느슨하게 해서 그 둘로
밀어 넣는 것은 자를 틀리게 대는 것"). 경계 변경은 `RULESET_VERSION` 상향을 부르고
`applyHysteresis` 이력을 무효화한다.

*재개 조건*: 세 안 중 택일 — (a) `STABLE_EARNINGS` 배당 조건 제거,
(b) `MATURE_INCOME` CAGR 상한을 5→15 로(다만 CAGR 11% 를 "성장 낮음"이라 부르는 것이 되고,
막대축인 주당배당금이 소스에 없어 커버리지는 오르지 않는다), (c) 441건 중 4건(0.9%)이므로 그대로 둔다.
**셋 다 화면을 깨지 않는다** — 슬롯 ③ 이 생략되고 승격 규칙이 작동한다.

---

## 3. 막힌 항목과 재개 조건

| 항목 | 무엇이 막고 있나 | 재개 조건(관측 가능하게) |
|---|---|---|
| **WO-SUB-05** | 법률 검토가 착수되지 않았다 | 검토 완료. 4축 등급은 스키마에 자리만 비워 뒀다(발행 스탬프 `axes: []`·`formula_version: null`) |
| **WO-SUB-04 A/B** | 트래픽. 현재 일 노출 최대 23·평균 5.8 로 상대 +30% 탐지에 **778일** | 일 노출 **500건**. 그때 `npx tsx scripts/ab-power.ts --rate=500` 재계산(+30% 를 36일에 잡는다) |
| **③ 유무 사후 비교** | 표본. 슬롯 구성 라벨 계측은 배선·프로덕션 검증 완료 | 두 군 **각각 노출 30건** |
| **WO-SUB-09 INV-06/11** | 02R 유예 상태 | 위 §2 의 경계 판단 결정 |
| **KR 위험요소 본문** | DART 본문 경로 미확인 | 별건 `WO-SUB-06-KR` |
| **KR 대손충당금 전입액** | DART 대손 계정 경로 미확인. US 개념에 억지 대응시키지 않았다 | KR 재무제표 계정명 실측. 위 KR 위험요소와 **같은 묶음이 싸다** |
| **02 일치율 재측정** | 골든셋 라벨 100개가 `STABLE_EARNINGS` **신설 이전**에 붙었다. 지금 재측정하면 분류기 변화와 라벨 노후가 섞인 숫자가 나온다 | **골든셋 재라벨링 먼저**(사람 작업, 100건). 그 뒤 `npx tsx scripts/archetype-goldenset.ts` — 100종목 팩트시트를 실시간 소스로 다시 빌드하므로 워크플로가 필요하다(현재 없음) |
| **C-1 입력 크기 A/B** | 유료 호출($0.15) + Groq TPD | 승인 + TPD 여유 |
| **03 골든셋 사람 검증** | 사람이 필요하다 | — |
| **종목 고유 리스크 합성 배치 미실행** | `symbol-risk-backfill.yml` 이 **한 번도 실행되지 않았다**(실행 이력 0). 저장된 리스크 레코드가 **1건**뿐이다(팩트시트 400건 대비). LLM 비용을 쓰므로 의도적 수동 실행만 하도록 만들어져 있다 | 비용 승인 + Groq TPD 여유. `gh workflow run symbol-risk-backfill.yml -f limit=6 -f rounds=20`. **화면은 깨지지 않는다** — "이 종목만의 리스크는 아직 확보하지 못했어요" 가 정직하게 뜬다 |
| **"2분기 연속 증가" 계절성** | 대손은 분기 계절성이 있다. 실측 12종목 중 3건이 `invalidated` 로 나왔다 | 발행 이력에서 이 조건의 `invalidated` 를 분기별로 분해. 계절성이면 전년동기 비교 개정안을 독트린 리뷰에 올린다(룰셋 버전 상향) |

---

## 3.5 성적표 실측 (2026-08-13, 판정기 1회 실행 후)

판정기 라우트(`/api/fomo/cron/business-invalidation`)는 **어떤 워크플로에도 붙어 있지 않다.**
DB 만 읽는 무료 연산이라 이번에 직접 호출해 PART A 효과를 확인했다.

| 지표 | 값 |
|---|---|
| 사업 무효 | n=28 · 충족 5 · 미충족 11 · 판정 불가 12 (도달률 17.9%) |
| 판정 불가 사유 | 전부 관측 부족(대손 관측 0건 등) — **미배선은 0** |
| 가격 무효선 | n=80 · **전부 판정 불가** |

**가격 무효선 80건이 전부 판정 불가인 것은 결함이 아니다.** 발행 스탬프는 WO-SUB-07 머지
이후(5일 전)부터 찍히고, 수익률 관측은 7일 창이 지나야 생긴다 — 스탬프가 찍힌 발행 중
첫 평가 시점에 도달한 것이 아직 없다. **2026-08-15 무렵부터 판정이 나온다.**
원장 전체에는 관측이 있다(7일 n=746 · 30일 n=19). 여기를 "안 되는 것" 으로 보고 고치려 들면
없는 문제를 만든다.

*재개 조건(판정기 자동화)*: 판정기를 수동 워크플로에 붙인다. `cron` 스케줄은 신설 금지
규약이 있으므로 `workflow_dispatch` 로 두거나 기존 일일 모니터에 스텝으로 넣는다.

---

## 3.7 WO-SUB-FILL — 화면 채우기 (2026-08-14)

**"만든 것이 돌지 않으면 만들지 않은 것과 같다"** 를 고친 라운드다. 정본 `docs/ops/CRON_WIRING.md`.

### 한 것

| 파트 | 결과 |
|---|---|
| 1-2 크론 전수 감사 | 라우트 14개 대조 → **완전 미배선 1건**(`business-invalidation`). `CRON_WIRING.md` |
| 1-1 판정기 배선 | Vercel Cron `50 21 * * *`. `ledger-outcomes`(40 21) **뒤** — 가격 무효선이 그 관측을 쓴다 |
| 1-3 `CRON_SECRET` | **미설정 실증**(인증 헤더 없이 200). 아래 §3.8 에 사람 조치 |
| 2 리스크 배치 스케줄 | `symbol-risk-backfill.yml` `30 0 * * *`(KST 09:30). 상태 4종 구분 |
| 2-3 진행 관측 | `GET /api/fomo/risk/coverage` 신설 |
| 3 슬롯② 완주 확인 | **정체 원인 규명** — 아래 |
| 4 C-1 입력 크기 A/B | **미실시(쿼터)** — §3.8 |
| 5 화면 확인 | 성적표 DOM 확인 · 덱 뎁스에 섹션이 **없던 것**을 찾아 붙였다 |

### 막고 있는 것은 하나다 — **Groq 일일 LLM 한도**

| 실측 | 값 |
|---|---|
| 리스크 레코드 | universe 441 · records 17 · **항목 있는 종목 0** |
| 실패 사유 | LLM 호출 실패 9 · 일일 쿼터 3 · DART corp_code 3 · SEC 추출 2 |
| 사업 실체(슬롯②) | 레코드 229 · slot2 12건(2.7%) · 지배적 실패 **`ai: 합성 호출 실패` 139건** |

슬롯② 2.7% 는 "완주 대기" 가 아니라 **같은 한도에 막힌 것**이다. 두 배치가 같은 풀을 쓴다.
400종목 × 약 1,486토큰 ≈ 59만 토큰이라 무료 티어로는 **6일 이상**이고, 그 사이 사업실체 배치와
경쟁한다. 크론이 걸렸으므로 이제 진척은 자동으로 쌓인다 — 다만 속도가 한도에 묶인다.

### 화면 확인 (PART 5)

| 지점 | 결과 |
|---|---|
| 성적표 무효 조건 | **DOM 확인** — 사업 무효 17.9% · 판단 28건 · 도달 5 · 미도달 11 · 판정 불가 12 · 사유 `관측 부족 12` |
| 카드 디테일 페이로드 | **API 확인** — 덱 10장 중 9장에 유형 리스크 3~4건 + 종목 리스크 미확보 사유 |
| 덱 뎁스 섹션 | **없었다 → 붙였다.** `WhereThisIsWrong` 이 `StockInsightView` 에만 있어 관심목록·검색·피드로만 보였다. 픽 카드의 "자세히" 는 `QuietPickDepth` 를 여는데 거기엔 없었다 |
| 덱 뎁스 DOM 재확인 | **미완** — 브라우저 페인의 React 상호작용이 반복 실패(두 탭·여러 방식). 회귀 테스트로 배선을 고정했고(섹션 제거 시 4건 실패 확인) DOM 확인은 남았다 |
| 종목 고유 리스크 표시 | **아직 없다** — 항목 있는 종목이 0이라 쿼터가 풀려야 뜬다 |

---

## 3.8 사람 조치가 필요한 것

| 항목 | 왜 사람인가 | 무엇을 하면 되나 |
|---|---|---|
| **`CRON_SECRET` 미설정** | 크론 라우트가 인증 없이 열려 있다(실증). `symbol-risk`·`business-context`(Groq)·`fundamentals`(SEC·DART·네이버)·prewarm 3종(TwelveData·Upbit)이 **외부 쿼터를 태운다** — 외부 반복 호출로 그날 배치를 죽일 수 있다 | Vercel 환경변수와 GitHub Secrets **양쪽**에 넣는다(한쪽만이면 워크플로 curl 이 401). 워크플로에 `Authorization: Bearer` 헤더 추가 필요 |
| **Groq 유료 전환 여부** | 무료 티어로는 400종목 채우기에 6일 이상, 두 배치가 경쟁한다. 비용 결정 | 전환하면 하루 안에 채워진다. 안 하면 크론이 6일에 걸쳐 채운다(둘 다 동작한다) |
| **배치 우선순위** | 리스크(00:30)가 하루 예산을 먼저 쓰고 사업실체(19:00)가 남은 것을 받는다. 어느 쪽이 먼저인지는 제품 판단 | 순서를 바꾸려면 두 워크플로의 cron 시각을 교환 |
| **C-1 입력 크기 A/B** | 승인됐지만 **오늘 쿼터가 없다.** 그리고 두 조건을 한 커밋으로 돌리려면 요청별 문자수 오버라이드가 필요하다(현재는 모듈 로드 시점 env) | 쿼터 리셋 후 `RISK_PROMPT_CHARS_OVERRIDE` 를 바꿔 두 번 배포하거나, 라우트에 실험용 `chars` 파라미터를 추가한다 |

---

## 4. 이번에 배운 함정 (되풀이 방지)

1. **"위반 0건"에는 두 원인이 있다** — 위반이 없어서 0 이거나, 검사가 그걸 못 봐서 0 이다.
   소급 스캔은 종목 리스크를 아예 읽지 않았고, 위반을 찾아도 종료 코드가 0 이었다.
   *규칙*: 검사를 만들면 **의도적 위반을 넣어 실패하는지** 본다.
2. **소스 문자열 스캔 테스트는 "무엇을 재는가"를 묻지 않는다.** 가격 무효선은 발행 시점 값끼리
   비교해 아무것도 재지 않으면서 타입·테스트를 통과했다.
   *규칙*: 집계·판정에는 행동 테스트를 둔다. 스캔은 보조다.
3. **지시서의 전제를 실측으로 확인하고 시작한다.** PART B 는 82건 전제로 2~3일이 잡혀 있었지만
   실제 공백은 4건이었다. 확인 비용은 워크플로 1회였다.
4. **개념 별칭은 최다 관측으로 고르면 시대가 잘린다**(CECL 전환). 합집합이 필요한 경우가 있다.
5. **낱말로 원인을 묶으면 해결한 일이 미해결로 보인다.** 대손 수집을 붙인 직후, 새 사유에
   "SEC **수집** 실패" 가 들어 있다는 이유로 `지표 수집 미배선` 버킷에 묶였다 — 배선한 것을
   화면이 미배선이라고 말했다. 원인 분포는 다음 작업을 정하는 입력이므로 이 오라벨은 같은 일을
   다시 하게 만든다. *규칙*: 버킷은 낱말이 아니라 **원인**으로 나눈다.
6. **비어 있는 수치를 결함으로 오인하지 않는다.** 가격 무효선 80건 전부 판정 불가는 아직
   평가 창에 도달한 발행이 없다는 뜻이다. 사유를 확인하기 전에 고치면 없는 문제를 만든다.
7. **API 응답과 화면 렌더는 다른 층이다.** `card-slots` 에 유형 리스크가 3~4건씩 실려 있어
   API 만 보면 "채워졌다" 로 읽혔지만, 덱 뎁스에는 섹션 자체가 없었다. WO-SUB-04 에서
   계측 라벨이 라우트 화이트리스트에 걸려 버려진 것과 같은 착오다.
   *규칙*: "화면에 뜬다" 는 **DOM 으로** 확인한다.
8. **낱말 감지를 또 틀렸다.** 쿼터 감지를 "쿼터" 라는 문자열로 짰는데, 실제 사유는
   `합성 호출 실패(HTTP 429)` 였다(일일 한도만 "일일 쿼터" 문구가 된다). 라운드 4개가 전부
   실패했는데 루프가 안 멈췄다. §4-5 와 같은 실수를 한 배치 안에서 두 번 했다.
   *규칙*: 상태 감지는 **실측 응답 여러 형태로 먼저 돌려본다**(이번엔 6가지로 검증했다).
9. **페이싱 상태는 서버리스 호출을 넘어 유지되지 않는다.** 라운드마다 새 인스턴스라
   in-process 감속이 초기화된다 — 워크플로가 3초 간격으로 몰아치면 매 라운드가 429 를 맞는다.
   배치를 청크로 쪼갤 때 페이싱을 청크 **사이**에도 둬야 한다.
10. **실패 레코드의 사유가 재시도 여부를 정한다.** 검증 전량 탈락이 "소스를 확보하지 못함" 으로
   저장되면 `RETRYABLE_REASON` 에 걸리지 않아 그 종목이 영구히 빈다. 사유는 화면 문구가
   아니라 **다음 실행의 입력**이다.
