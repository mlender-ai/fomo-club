# CTX-00 PART A — 카드 페이로드 필드 인벤토리

| 항목 | 값 |
|---|---|
| 조사 | **2026-08-17 16:52 UTC** · 프로덕션 읽기 전용 GET |
| 수단 | `/api/fomo/ops/audit/card-fields` (`AUDIT_TOKEN`). **`AUDIT_TOKEN` 이 설정돼 동작 확인됨** |
| 모집단 | **덱 27개 · 픽 256장** (`quiet-pick:2026-07-23` ~ `quiet-pick:2026-08-18`) |
| 표본 | **200장** · 시드 `ctx-00` · SHA-256 결정론 추출 |
| 표본 판정 | ✅ **스펙 200장 충족** (직전 판은 10장이었다) · 잔여 한계는 §5 |
| 직전 판 | 2026-08-16 · 발행 카드 10장. 이 문서가 그것을 **대체**한다 |

집계 전에 응답 원문 구조를 먼저 덤프하고(`mode=dump`, 5장) 그 구조를 근거로 집계했다 —
CTX-00 §0 「구조 덤프 선행」 규율. 추측 파서로 0% 를 박제하지 않는다.

---

## 0. 10장 → 200장으로 바뀐 판정 — 먼저 읽을 것

표본을 20배로 늘리자 **두 가지가 뒤집혔다.** 10장 표본을 믿고 설계했다면 둘 다 틀렸을 것이다.

| 항목 | 10장 판정 | 200장 실측 | 의미 |
|---|---|---|---|
| `foreign_streak` · `multi_cluster` 발행 | **0장** — "정의는 있고 발행이 없다" | `foreign_streak` **20** · `multi_cluster` **22** | 발행된다. 10장은 그날 덱의 우연이었다 |
| 훅↔디테일 연속일수 불일치 | 2장 (institution_streak 전부) | **29 / 200 (14.5%)** | 2장짜리 이상이 아니라 **계통 결함** |

> 표본이 작으면 **없는 것을 없다고 말하게 된다.** 위 첫 줄이 그 사례다.
> 반대로 뒤집히지 **않은** 항목들(주체 분리 0, anomaly 수치 0, 배경 0, OHLC 0)은
> 200장에서도 그대로이므로 이제 **구조적 사실**로 확정된다.

---

## 1. A-1 축별 실측 (n=200)

### 수급 주체 — **주체 분리가 페이로드에 없다** (확정)

| 확인 | 실측 |
|---|---|
| 주체별 분리 필드(`individualNet`·`foreignNet`·`institutionNet` 중 하나라도) | **0 / 200 (0%)** |
| `signal.actors` 자료형 | **문자열 하나** |
| `signal.kind` 분포 | `insider_cluster` **106** · `institution_streak` **52** · `multi_cluster` **22** · `foreign_streak` **20** |

`signal.actors` 값 분포 (12종):

| 값 | n | 값 | n |
|---|---|---|---|
| 기관 | 52 | 임원 2명 | 12 |
| 내부자 4명 | 24 | 내부자 5명 | 8 |
| 내부자 3명 | 23 | 임원 3명 | 8 |
| **외국인·기관** | **22** | 내부자 16명 | 7 |
| 외국인 | 20 | 임원 7명 | 4 |
| 내부자 2명 | 18 | 임원 5명 | 2 |

**주체가 데이터가 아니라 표시 문자열이다.** `"외국인·기관"` 22장은 두 주체가 한 문자열에
들어간 경우로, **기계적으로 분해할 수 없다.** `"내부자 N명"`·`"임원 N명"` 은 사람 수를
문자열에 녹여 놓았다 — 숫자를 다시 꺼내려면 파싱해야 한다.

**개인(개미) 카드는 200장 중 0장이다.** CTX-01 의 개미 축은 입력 자체가 없다.

### 연속일수 — **주체별로 없다. 스칼라 하나다** (확정)

| 확인 | 실측 |
|---|---|
| `signal.days` 스칼라 | 있음 |
| 주체별 연속일수 배열 | **0 / 200** |
| **훅(`signal.days`) ↔ 디테일(`whyNow.summary`) 불일치** | **29 / 200 (14.5%)** |

한 카드는 **한 주체의 연속일수 하나**만 갖는다. "기관은 26일, 외국인은 3일" 을 표현할 자리가 없다.

불일치 14.5% 는 **10장 표본이 2장으로 잡아낸 것의 진짜 규모**다. 훅은 "26일째", 디테일은
"20일 연속" 처럼 **한 화면에서 두 숫자가 충돌한다.** 두 값이 다른 계산 경로에서 오고 한쪽이
갱신되지 않는다. 기여도 판정(A-2) 이전에 **신뢰의 문제**다.

### 거래량 — **수치가 문장 안에 녹아 있다** (확정)

| 확인 | 실측 |
|---|---|
| `anomalies[]` 종류 분포 | frequency **128** · silence **122** · scale **73** · near_low **65** · vacuum **62** · participants **43** |
| anomaly 항목의 **수치 필드** | **0 / 200** — 키는 `kind`·`text`·`chip`·`axis`·`strength` 뿐 |

엔진은 실수치를 계산한다 — `QuietPickAnomalyFacts`(`apps/web/lib/quiet-pick-hook.ts`)에
`volumePct` · `volumeVacuumRatio` · `pctAboveYearLow` · `mcapPct` · `mentionCount` ·
`isLongestStreak` · `streakWindowDays` 가 있다. **페이로드에는 그 숫자가 남지 않고 문장만 남는다:**

> `"거래가 평소의 30%로 말라 있었어요"` ← `volumeVacuumRatio = 0.3` 이 프로즈로 소실

**신규 수집 없이 회수 가능한 값이다.** 발행 시점에 facts 를 같이 박제하면 된다.
`signalFacts` 키가 **21 / 200** 에 있다 — 최근 발행분에만 붙은 부분 조치(F-2)이고, 과거분엔 없다.

### 가격 구조 — 종가 스파크라인만

| 확인 | 실측 |
|---|---|
| `price.sparkline` 길이 | **66 고정** (min=p50=max=66) |
| 자료형 | **종가 1차원 배열** — OHLC 아님 |
| `conviction.whyNow.phase` | **128 / 200 (64%)** |
| `conviction.whyNow.keyLevels` | **40 / 200 (20%)** |

### 캔들·OHLC — **시계열이 카드에 없다** (확정)

| 확인 | 실측 |
|---|---|
| OHLC·거래량 시계열 필드 | **0 / 200** |
| `dataQuality` 키 존재 | 193 / 200 |
| `dataQuality.candles` 분포 | min **251** · p50 **260** · max **260** |
| `candles ≥ 250` | **193 / 193** (`dataQuality` 보유분 전수) |

`dataQuality.candles` 는 **개수(정수)**지 시계열이 아니다. 카드에서 일봉을 다시 그릴 수 없다.
캐시 상한은 `KR_CANDLE_KEEP_DAYS = 260`(`apps/web/lib/kr-candle-cache.ts:21`).

> ⚠️ **193/193 을 유니버스 확보율로 읽으면 안 된다.** 발행 게이트가 `candles < 200` 을 이미
> 걸러낸다(`cron/quiet-pick/route.ts`). 이것은 게이트 통과분의 성질이다 — §5, B-2 는 §4.

### 배경(뉴스·공시·실적) — **필드가 하나도 없다** (확정)

| 확인 | 실측 |
|---|---|
| 뉴스·공시·실적·촉매 관련 필드 | **0 / 200** |
| `signalStats` | **0 / 200** |

픽 최상위 키 전수(200장 합집합, 괄호는 보유 장수):

`hook`(200) · `price`(200) · `signal`(200) · `subject`(200) · `conviction`(200) ·
`qualifiedAt`(200) · `companyScore`(200) · `invalidation`(200) · `anomalies`(196) ·
`dataQuality`(193) · `chips`(30) · `signalFacts`(21) · `liquidityNote`(17)

**"왜 사고 있나" 를 담을 자리가 스키마에 존재하지 않는다.** CTX-03·04 는 신규 필드가 필요하다.

---

## 2. A-2 판단 기여도 — 사람 판정 대기

CTX-00 은 A-2 를 **"사람이 판정하고 근거를 한 줄씩 남긴다"** 로 규정한다.
에이전트가 대신 판정하지 않는다. 위 인벤토리가 그 판정의 입력이다.

블록 목록(200장 실측 보유율):
`hook`(100%) · `price/sparkline`(100%) · `signal`(100%) · `invalidation`(100%) ·
`conviction.whyNow`(100%, 단 `phase` 64% · `keyLevels` 20%) · `conviction.committee`(100%) ·
`anomalies`(98%) · `chips`(15%) · `liquidityNote`(8.5%) · `signalStats`(0%).

> A-2 판정 시 **훅↔디테일 불일치 14.5%** 를 반드시 고려할 것 — 숫자가 서로 다른 블록은
> 기여도 이전에 신뢰의 문제다.

---

## 3. 함께 관측된 커버리지 (유니버스 **486**, 2026-08-17 실측)

직전 판이 `미측정` 으로 남긴 두 라우트를 **로컬에서 측정했다.** 60초 제약은 MCP 도구의 것이었고
라우트 자체는 90~97초에 200 을 준다.

| 라우트 | 소요 | 실측 |
|---|---|---|
| `fundamentals/coverage` | **97.1s** | records **407 / 486** (KR 286 · US 121) |
| `business-context/coverage` | **88.8s** | records **313 / 486** · 배지 충분 **0** · 보통 15 · 낮음 53 · 없음 **245** |
| `card-slots/coverage` | **94.9s** | slot2 **67 / 486** · slot3 **352 / 486** |
| `risk/coverage` | 4.5s | records 25 · withItems **0** · ratePct **0** · untouched 461 |

### fundamentals — 최저 확보율 필드

| 필드 | KR (n=286) | US (n=121) |
|---|---|---|
| `consensus.revenue_fy1` | 49.0% | **0%** |
| `valuation.dividend_yield` | 48.6% | **17.4%** |
| `valuation.per_forward` | **47.9%** | 77.7% |
| `consensus.eps_fy1` | 50.7% | — |
| `cashflow.dividend_paid_ttm` | 54.5% | 33.9% |
| `valuation.per_ttm` | 62.9% | 65.3% |
| (최고) `fiscal.annual` / `classification.industry` | 93.7% | 100% |

**US 컨센서스 매출은 0% 다.** CTX-04 가 컨센서스를 전제하면 US 는 설계에서 빠진다.

### business-context — 실패 사유

`ai: 합성 호출 실패` **202** 가 압도적이고, 그 뒤로 `chunk: 예산 초과로 N개 제외` 가
50건 이상 흩어져 있다. `naver: corporationSummary 없음` 11 · `sec: 섹션 추출 실패` 10 ·
`universe: 종목코드 없음` 6 · `naver: 종목코드 형식 오류` 5.

소스 종류: `vendor_summary` 58 · `disclosure` 25.
**slot3 은 KR·US 양쪽 0 이다**(KR slot1 50 / slot2 8 / slot3 0, US 17 / 8 / 0).

### risk — 여전히 0%

`LLM 호출 실패` 12 · `일일 LLM 쿼터 소진` 5 · `DART corp_code 매핑 없음` 4 ·
`sec: 위험요소 추출 실패` 3 · `합성이 후보를 내지 못함` 1.

**→ LLM 쿼터·호출 실패가 커버리지의 지배적 병목이다.** 수집 소스 문제가 아니다.

---

## 4. B-2 유니버스 일봉 확보율 — **≥120일은 실측, ≥250일은 미확인**

CTX-00 B-2 는 **≥250거래일** 확보율을 요구한다. 그 숫자는 아직 못 낸다. 이유를 정확히 적는다.

측정 수단은 `kr-candle-prewarm` 워크플로의 실행 산출물이다(쓰기를 새로 유발하지 않고
**이미 성공한 실행의 출력**을 읽었다 — CTX-00 「크론 호출 금지」 준수).

최근 성공 실행(2026-08-15, run `31836259081`):

```
universe 450 · stored 447 · short 3 · failed 0 · skippedForBudget 0 · 31.5s
```

| 항목 | 판정 |
|---|---|
| KR **≥120거래일** 확보율 | **447 / 450 = 99.3%** (실측) |
| KR **≥250거래일** 확보율 | **미확인** |
| 거래량 결측률 | **미확인** |
| 수정주가(분할·병합 조정) 여부 | **미확인** — 분할 미조정 의심 3건이 별건으로 등재돼 있다 |

**왜 ≥250 을 못 내는가**: 프리웜이 `candles.length >= 120` 을 기준으로 저장/미저장을 세고
(`cron/kr-candle-prewarm/route.ts:49`, `MIN_USEFUL_DAYS = 120`), 그 이상은 구간을 나누지 않는다.
`stored 447` 은 "120일 이상"이지 "250일 이상"이 아니다. **250 을 120 에서 추정하지 않는다.**

**필요한 도구**: 유니버스별 캔들 개수 분포를 내는 읽기 전용 커버리지 라우트 하나.
CTX-00 R1 이 읽기 전용 감사 라우트 추가를 허용하므로 규약상 가능하다. 이번 범위 밖이라 만들지 않았다.

US 쪽은 `us-market-prewarm` 이 `fetched 259 / written 259 / rowsWithSparkline 47` 형태로
스파크라인 기준을 보고해 **일봉 개수 기준과 대응하지 않는다** → US ≥250 도 **미확인**.

---

## 5. 표본 한계 — 충족분과 잔여분을 구분해 적는다

| 스펙 요구 | 실제 | 판정 |
|---|---|---|
| 200장 | **200장** (모집단 256) | ✅ 충족 |
| 시드 기록 | `ctx-00` · SHA-256 결정론 | ✅ 충족 |
| **유니버스** 무작위 추출 | **아님 — 발행 게이트 통과분에서 추출** | ⚠️ **미충족** |

**남은 편향은 하나이고, 그것이 무엇을 왜곡하는지 특정할 수 있다.**

모집단 256장은 27일치 **발행된** 픽 전수다. 발행 게이트가 `candles < 200`·`no_invalidation`·
`mega_cap` 등을 이미 걸러낸다. 따라서:

- **`dataQuality.candles` 는 실제보다 좋게 보인다** → §4 로 대체해 읽을 것. 유니버스 확보율로 읽으면
  CTX-00 실패 모드 「표본에 대형주를 섞어 확보율이 좋아 보임」에 정확히 해당한다.
- **주체 분리 0 · anomaly 수치 0 · 배경 0 · OHLC 0 은 이 편향과 무관하다.** 게이트가 통과시킨
  카드에서조차 0 이므로, 유니버스로 넓혀도 0 보다 나아질 수 없다 — **하한이 아니라 상한이다.**
- `signal.kind` 분포·`actors` 분포·훅 불일치율은 **발행분의 성질**로 읽어야 한다(유니버스 성질이 아니다).

CTX-00 개정 R3 은 「확보 불가 시 확보 가능한 전수를 쓰고 한계를 명시」를 허용한다.
200장은 확보됐고, **유니버스 추출은 공개·감사 라우트 어느 쪽으로도 불가**하다 —
발행되지 않은 종목에는 카드가 애초에 없다. 그것이 이 한계의 성질이다.

---

## 6. 남은 일

1. **A-2 사람 판정** — 블록별 기여도. 위 보유율이 입력이다.
2. **B-2 ≥250거래일 유니버스 확보율** — 캔들 개수 분포 라우트가 필요하다(§4).
3. **거래량 결측률 · 수정주가 여부** — 미확인.
4. **훅↔디테일 불일치 14.5%** — 계통 결함으로 등재됨. 수정은 CTX-05 범위.
