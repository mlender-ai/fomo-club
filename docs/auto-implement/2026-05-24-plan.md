아래는 주어진 실행 지시를 기반으로 "카드 의미 미니 스토리텍스트 추가"를 구현한 세부 내용입니다.

---

### 1. `packages/shared`에서 공용 타입 및 새로운 스토리 데이터 정의

=== FILE: packages/shared/types/tarot.ts ===
```typescript
export type TarotCard = {
  id: string;
  name: string;
  meaning: string;
  miniStory: string; // 새로운 필드
};
```

=== FILE: packages/shared/constants/tarotCards.ts ===
```typescript
import { TarotCard } from "../types/tarot";

export const TarotCards: TarotCard[] = [
  {
    id: "1",
    name: "The Fool",
    meaning: "New beginnings, adventure, free spirit.",
    miniStory: "You stand on the edge of possibility, ready to leap into the unknown. Trust your instincts and embrace the journey ahead.",
  },
 
