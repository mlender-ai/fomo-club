### 구현 계획
사용자 몰입감을 높이기 위해, 결과 화면에서 **카드의 의미와 관련된 미니 스토리텍스트**를 추가합니다. 카드를 구분할 수 있는 기존 타입을 활용하며, 공용 타입 및 비즈니스 로직은 `packages/shared`와 `packages/tarot-core`에 정의합니다. 추가된 정보는 `ResultCard` 컴포넌트에서 표시합니다.

---

### 변경사항

#### 1. **스토리 텍스트 타입 추가**

=== FILE: packages/shared/types/tarotCard.ts ===
```diff
diff --git a/packages/shared/types/tarotCard.ts b/packages/shared/types/tarotCard.ts
export interface TarotCard {
  id: string;
  name: string;
  description: string;
  element: string;
  meaning: string;
+ miniStory: string; // 🆕 각 카드의 관련 스토리텍스트
}


