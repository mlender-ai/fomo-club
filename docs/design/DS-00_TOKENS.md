# DS-00 — 디자인 토큰·원칙

| 항목 | 값 |
|---|---|
| 범위 | 전 화면 공통 기반 |
| 선행 | 없음. 이 문서가 나머지 6개의 기준 |
| 대상 | 모바일 우선 (앱 출시 전제), 웹은 동일 스펙을 중앙 정렬 |
| 상태 | **v2 기반 정본 (2026-08-19 등재).** DS-01(메인 카드)·DS-02(덱)·DS-03(상세)·DS-04(성적표·내 기록)·DS-05(결손·빈 상태) 등재·구현 완료 · DS-06 미도착 |
| 권위 | 색·타이포·간격·형태·터치·금지 목록은 **이 문서가 최상위** — 단 **기획자 모킹이 이 문서보다 위다**(문구와 모킹이 어긋나면 모킹을 따르고 이 문서를 고친다). `docs/DESIGN.md`의 해당 절은 이 문서에서 파생된 미러이며, 충돌 시 DS-00이 이긴다. 모션·모티프·컴포넌트 서술은 DS-01~06 도착 전까지 `docs/DESIGN.md`가 계속 담당한다 |
| 기계가독 소스 | `design/tokens.json` → `ds` 블록 (DTCG) |
| 화면별 스펙 | DS-01 메인 카드 → `docs/design/DS-01_MAIN_CARD.md` · DS-02 덱 화면 → `docs/design/DS-02_DECK.md` · DS-03 상세 → `docs/design/DS-03_DETAIL.md` · DS-04 성적표·내 기록 → `docs/design/DS-04_RECORDS.md` · DS-05 결손·빈 상태 → `docs/design/DS-05_MISSING.md` |

---

## 1. 두 가지 원칙

```
메인 카드 = 후킹      한 장면에 놀라움 하나. 텍스트를 최대한 없앤다.
상세 페이지 = 설득    근거를 쌓되 스캔 가능하게. 순서가 곧 논증이다.
```

이 원칙에 안 맞는 요소는 **크기를 줄이는 게 아니라 다른 화면으로 옮기거나 삭제**한다.

### 1-1. 존재하지 않는 기능의 UI를 만들지 않는다

현재 데이터로 채울 수 없는 것은 **자리도 만들지 않는다.**

| 있음 | 없음 (UI 만들지 말 것) |
|---|---|
| 훅 문장, 신호 근거 수치 | 주체 3분 (개인/기관/외국인 동시) |
| 스파크라인, 무효선 | 가격·거래량 구조 판정 |
| 회사 설명(벤더 요약) | 배경 후보 ("왜 사는가") |
| 시총·PER·EPS·PBR | 종목 고유 리스크 (대부분 미확보) |
| 최근 30일 재료 유무 | 5년 밴드 위치 (대부분 미확보) |
| 유형 리스크 템플릿 | 검증실 |
| 판단 원장 기록(발행일·당시가) | |

---

## 2. 색

앱은 **다크 전용**이다. 라이트 모드를 만들지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `bg` | `#000000` | 화면 배경 |
| `surface-1` | `#101010` | 카드 |
| `surface-2` | `#181818` | 카드 안 블록 |
| `border` | `#242422` | 구분선 (0.5px) |
| `text-1` | `#FFFFFF` | 본문·제목 |
| `text-2` | `#9A9A96` | 보조 |
| `text-3` | `#5A5A57` | 캡션·비활성 |
| `accent` | `#D4FF3F` | 결론 · 성적 · CTA (§2-1) |
| `accent-ink` | `#1A1A00` | accent 배경 위 텍스트 |
| `down` | `#7A7A76` | 하락 (빨강 쓰지 않음) |

### 2-1. accent 사용 규칙 — 가장 중요

> **accent 는 결론 · 우리 성적 · CTA 세 자리에 쓴다.**

기획자 모킹(2026-08-20) 기준으로 **개정했다.** 종전 문구는 "한 화면에 accent 는 한 곳(우리 성적)"
이었고 CTA 를 금지했다. 그대로 구현하니 화면 전체가 무채색으로 죽었다 — 결론이 눈에 먼저
닿지 않고, 채워진 버튼이 없어 다음 행동이 보이지 않았다.

| 자리 | 쓰는 이유 |
|---|---|
| **결론** (카드 `display`) | 이 카드에서 놀라운 것이 무엇인지 가장 먼저 닿아야 한다 |
| **우리 성적** (좌측 바 + 수익률) | 이 앱을 믿을 만한가에 대한 답 |
| **CTA** (`accent` 배경 + `accent-ink` 글씨) | 다음 행동. 화면에서 유일하게 채워진 버튼이다 |

| 화면 | accent |
|---|---|
| 메인 카드 | 결론 · 우리 성적 · CTA |
| 상세 | 우리 기록의 수익률(좌측 바 포함). 상세에는 하단 CTA 가 없다 |
| 성적표 | 대표 지표 하나 |
| 지켜보는 중 · 헤더 · 덱 타이틀 · 하단 탭 | 없음 |

**여전히 accent 를 쓰지 않는 곳** — 스파크라인·차트(회색 `text-2` 선), 등락(상승 `text-1` /
하락 `down`), 근거 박스, 섹션 제목, 칩, 헤더 로고.

**성적이 없는 카드(오늘 첫 발행)는 성적 accent 가 없다.** 그건 정상이다. 색을 다른 데로 옮기지 않는다.

---

## 3. 타이포

| 폰트 | 용도 |
|---|---|
| Departure Mono | 티커, 종목코드, 수치, 라벨, 날짜 |
| Pretendard | 문장 전부 |

수치와 문장을 폰트로 분리한다. 이것이 "Living Terminal × Clean Sheet"의 실행 방식이다.

### 3-1. 스케일

| 이름 | px / weight / line-height / letter-spacing | 용도 |
|---|---|---|
| `display` | 24 / 500 / 1.32 / -0.02em | **결론 문장.** 카드당 1회 |
| `display-sm` | 22 / 500 / 1.32 / -0.02em | 상세의 결론 (DS-03 §4) — 카드보다 한 단 낮다 |
| `title-lg` | 20 / 500 / 1.32 / -0.01em | 덱 타이틀 (DS-02 §3) |
| `title` | 17 / 500 / 1.4 / -0.01em | 종목명, 섹션 제목 |
| `body` | 14 / 400 / 1.65 / 0 | 설명 문장 |
| `data` | 14 / 400 / 1.4 / 0 (mono) | 수치 |
| `label` | 12 / 400 / 1.4 / 0.04em (mono) | 라벨, 섹터 |
| `caption` | 11 / 400 / 1.5 / 0.02em | 출처, 주석 |

**11px 미만 금지. weight는 400과 500 두 개만.**

`display-sm`·`title-lg` 는 DS-02/DS-03 이 요구해 **추가한 단**이다. 스케일은 닫힌 목록이 아니라, 화면 스펙이 필요로 하면 이 표에 등재하고 쓴다 — 등재 없이 임의 px 을 쓰는 것만 금지다.

### 3-2. 문장 규칙

- 해요체 통일. `~하였음`, `~합니다` 금지
- 한 문장 최대 2줄. 3줄 넘으면 문장을 자른다
- 마침표: 본문에는 찍고, 라벨·제목·버튼에는 안 찍는다

---

## 4. 간격

4의 배수만 사용한다.

| 토큰 | px | 용도 |
|---|---|---|
| `s1` | 4 | 라벨-값 |
| `s2` | 8 | 인접 요소 |
| `s3` | 12 | 블록 내부 |
| `s4` | 16 | 카드 패딩, 블록 간 |
| `s5` | 24 | 섹션 간 |
| `s6` | 32 | 큰 구획 |

- 카드 패딩: `s4` (16px)
- 화면 좌우 여백: 20px
- 섹션 사이: `s5` (24px) + 0.5px 구분선

---

## 5. 형태

| 요소 | 값 |
|---|---|
| 카드 | radius 16px, `surface-1`, 테두리 없음 |
| 카드 내부 블록 | radius 10px, `surface-2` |
| 버튼 (주요) | radius 999px, height 48px |
| 버튼 (보조) | radius 999px, height 44px, 0.5px `border` |
| 칩 | radius 999px, height 26px, padding 0 10px |
| 구분선 | 0.5px `border` |

**그림자·그라데이션·글로우 전면 금지.** 깊이는 배경 밝기 차이로만 만든다.

---

## 6. 터치 타겟

앱 출시 전제이므로 강제한다.

- 최소 44×44pt
- 인접 타겟 간 최소 8px
- 카드 전체가 탭 영역 (별도 버튼 없이도 상세로 진입)

---

## 7. 금지 목록

| 금지 | 사유 |
|---|---|
| accent 를 §2-1 세 자리 밖에 쓰기 | 위계 붕괴 |
| 같은 숫자 2회 이상 반복 | 소음 |
| 빈 헤더 (제목만 있고 본문 없음) | 고장으로 읽힘 |
| 고정 높이로 생긴 공백 | 내용에 따라 높이가 변해야 함 |
| 3개 이상의 CTA | 선택 마비 |
| 이모지 | 톤 불일치 |
| 빨강/초록 등락 색 | 다크 배경에서 피로 |
| 와이코프·클러스터 등 은어 | 왕초보 대상 |

---

## 8. 산출물

- `packages/design-tokens/` 또는 Tailwind config에 위 토큰 전량 반영
- 하드코딩된 색·px를 전부 토큰 참조로 교체
- 토큰 외의 값이 쓰이면 lint 실패

---

## 9. v1(기존 DESIGN.md) 대비 충돌 해소 — 등재 시점 기록

DS-00은 신규 문서가 아니라 **v1의 업그레이드**다. 아래는 v1과 값이 달라진 항목과 판정이다.

| 항목 | v1 (`docs/DESIGN.md`) | DS-00 (v2) | 판정 |
|---|---|---|---|
| 배경/표면 | canvas `#0B0B0C` / surface `#141416` / raised `#1A1A1D` / overlay `#202024` | `bg #000000` / `surface-1 #101010` / `surface-2 #181818` | DS-00. 3단으로 축약 |
| 보더 | `rgba(255,255,255,.08)` 1px | `#242422` 0.5px | DS-00 |
| 텍스트 | `#F2F2F0` / `#8A8A86` / `#5A5A57` | `#FFFFFF` / `#9A9A96` / `#5A5A57` | DS-00 |
| 액센트 | 오렌지(열기) + 네온(발견) **2축** | `accent #D4FF3F` **1축, 화면당 1회** | DS-00. 오렌지 축 폐기 |
| 등락색 | 봉인색 `up #FF4D4D` / `down #4D8DFF` | 색 없음. 상승=`text-1`, 하락=`down #7A7A76` | DS-00 (§7 "빨강/초록 등락 색" 금지) |
| 감정색 5종 | 카드 포인트 색 | 미사용 | **레거시.** `@fomo/core` `EMOTION_COLORS`는 데이터 계약으로 남고 UI에서 쓰지 않는다 |
| 그린/레드플래그 색 | 보조 큐 허용 | 미사용 (라벨 텍스트만) | DS-00 |
| 글로우 | 스와이프 시 기능적 네온 글로우 허용 | **전면 금지** | DS-00 |
| 타이포 스케일 | display-xl 56 … micro 11 | display 24 / title 17 / body 14 / data 14 / label 12 / caption 11 | DS-00. 최소 11px은 동일 |
| weight | 400·500·700 | **400·500만** | DS-00 |
| 간격 | 4·8·12·16·20·24·32·40·48 | `s1~s6` (4·8·12·16·24·32), 화면 여백 20 | DS-00. 40·48 폐기 |
| radius | sm8 / md12 / lg16 / pill | 카드 16 / 블록 10 / pill 999 | DS-00. 블록 10 신설 |
| 폰트 | Departure Mono + Pretendard | 동일 | 변화 없음 (에셋 미확보 — §10) |
| 라이트 모드 | "범위 밖(미래)" | **만들지 않는다** | DS-00 |

계속 유효한 v1 조항(DS-01~06 도착까지 `docs/DESIGN.md` 담당): 모션·제스처(§7), 데이터 비주얼 프리미티브(§8, 단 색은 DS-00), 컴포넌트 구성 순서(§9), 보이스·톤(§10), 거버넌스(§13).

## 10. 미해결 (등재 시점)

- **폰트 에셋**: Departure Mono는 CDN 부재(OFL zip). `@font-face` 셋업 전까지 mono 스택은 폴백(JetBrains Mono → Pretendard)으로 동작한다. 수치/문장 폰트 분리는 에셋 셋업 후 완결된다.
- **하드코딩 스윕(§8)**: 화면 단위로 진행한다. **메인 카드(DS-01)·픽 탭 셸과 덱(DS-02)·상세(DS-03)·성적표·내 기록(DS-04) 완료** — 해당 컴포넌트는 `ds` 토큰만 쓴다. 남은 화면(검색·기타)은 해당 DS 문서 도착 후. 지금 값만 바꾸면 미도착 스펙과 두 번 충돌한다.
- **lint 게이트(§8)**: 토큰 외 값 사용 시 실패시키는 규칙은 스윕이 끝난 화면 비중이 늘어난 뒤 켠다. 현재는 `ds` 토큰 ↔ 코드 드리프트 테스트 + 화면별 DS 테스트(`quietCardDs01.test.ts`, `quietDeckDs02.test.ts`, `e2e/quiet-card.spec.ts`, `e2e/quiet-deck.spec.ts`)로 지킨다.
